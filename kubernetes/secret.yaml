apiVersion: v1
kind: Secret
metadata:
  name: frodo
  namespace: <replace-with-namespace>
type: Opaque
data:
  # echo -n "yourpassword" | base64
  QUARKUS_DATASOURCE_PASSWORD: <replace-with-base64-encoded-password>
