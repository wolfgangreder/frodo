{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "frodo.serviceAccountName" . }}
  labels:
    {{- include "frodo.labels" . | nindent 4 }}
{{- end }}
