apiVersion: v1
kind: Secret
metadata:
  name: {{ include "frodo.fullname" . }}
  labels:
    {{- include "frodo.labels" . | nindent 4 }}
type: Opaque
data:
  QUARKUS_DATASOURCE_PASSWORD: {{ .Values.firebird.password | b64enc | quote }}
